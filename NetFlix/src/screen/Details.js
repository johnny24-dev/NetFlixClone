import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const Details = () => {
  return (
    <View>
      <Text>Details movies</Text>
    </View>
  );
};

export default Details;

const styles = StyleSheet.create({});
